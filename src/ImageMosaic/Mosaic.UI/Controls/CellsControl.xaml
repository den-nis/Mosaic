<UserControl x:Class="Mosaic.UI.Controls.CellsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:viewmodels="clr-namespace:Mosaic.UI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:CellsViewModel}"
			 mc:Ignorable="d"
			 d:Background="White" MinWidth="150">

	<StackPanel Grid.Column="1">
		<Label>Cells</Label>
		<Label Margin="0,20,0,0">Columns:</Label>
		<Slider Minimum="4" Maximum="200" TickFrequency="4" Value="{Binding Path=Columns}"/>
		<Label>Rows:</Label>
		<Slider Minimum="4" Maximum="200" TickFrequency="4" Value="{Binding Path=Rows}"/>
		<Label>Resolution:</Label>
		<Slider Minimum="8" Maximum="256" TickFrequency="8" Value="{Binding Path=Resolution}"/>
		<Label>Info:</Label>
		<Label Margin="20,0,0,0" Content="{Binding Path=Info}"/>
	</StackPanel>
</UserControl>

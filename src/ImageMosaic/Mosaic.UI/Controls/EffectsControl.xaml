<UserControl x:Class="Mosaic.UI.Controls.EffectsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:viewmodels="clr-namespace:Mosaic.UI.ViewModels" 
			 d:DataContext="{d:DesignInstance Type=viewmodels:EffectsViewModel}"
			 mc:Ignorable="d" 
             d:Background="white" MinWidth="200">
	<StackPanel Grid.Column="2">
		<Label>Effects</Label>
		<Label Margin="0,20,0,0">Contrast:</Label>
		<Slider Minimum="-50" Maximum="50" Value="{Binding Path=Contrast}"/>
		<Label>Brightness:</Label>
		<Slider Minimum="-50" Maximum="50" Value="{Binding Path=Brightness}"/>

		<Label>Tint:</Label>
		<StackPanel Margin="10">
			<Label>Red:</Label>
			<Slider Minimum="0" Maximum="255" Value="{Binding Path=Red}"/>
			<Label>Green:</Label>
			<Slider Minimum="0" Maximum="255" Value="{Binding Path=Green}"/>
			<Label>Blue:</Label>
			<Slider Minimum="0" Maximum="255" Value="{Binding Path=Blue}"/>
		</StackPanel>
	</StackPanel>
</UserControl>
